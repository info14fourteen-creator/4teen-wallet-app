import StubScreen from '../src/ui/stub-screen';

export default function HomeScreen() {
  return (
    <StubScreen
      eyebrow="HOME"
      title="Personal Home"
      body="This screen will become the new personal home surface. Wallet is now separated into its own dedicated screen."
    />
  );
}
